# Spellpaw Server — Environment Variables
JWT_SECRET=your-secret-key-change-me
PORT=3002
CLIENT_ORIGIN=http://localhost:5173
# Tool Server endpoint (front-end Vite dev server). Must match the port your browser is using.
TOOL_SERVER_ENDPOINT=http://127.0.0.1.nip.io:5173/tool
